{
	"name": "Prettier GitHub",
	"description": "Format code in GitHub comments with `prettier`",
	"repository": "https://github.com/jgierer12/prettier-github",
	"env": {
		"GITHUB_TOKEN": {
			"description": "Your GitHub personal access token"
		},
		"GITHUB_WEBHOOK_SECRET": {
			"description": "Your GitHub webhook secret",
			"generator": "secret"
		},
		"GITHUB_OPTIONS": {
			"description": "JSON-encoded options object for the `node-github` client",
			"value": "{}"
		},
		"PRETTIER_OPTIONS": {
			"description": "JSON-encoded options object for `prettier`",
			"value": "{}"
		},
		"LANGS": {
			"description": "JSON-encoded array of languages that should be formatted",
			"value": "[\"js\", \"javascript\", \"jsx\", \"es6\", \"css\", \"less\", \"scss\", \"ts\"]"
		}
	}
}
